{
  "name": "tsl",
  "version": "1.6.0",
  "scripts": {
    "build": "mkdir -p css && sass scss:css",
    "dev": "mkdir -p css && sass --watch scss:css",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "sass": "^1.49.11"
  }
}
